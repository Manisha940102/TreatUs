import { MenuListItem } from "./MenuListItem";
import { PickedItem } from "./PickedItem";

export class TreatUsers{   
    name: string;  
    email:string; 
    phoneNumber:string;
    isActive:boolean;
    pickedItem: MenuListItem[]; 
    
}